apiVersion: batch/v1
kind: Job

metadata:
  name: sleep-hook

  annotations:
    argocd.argoproj.io/hook: PreSync

spec:
  template:
    spec:
      containers:
      - name: sleep
        image: busybox
        command:
          - sh
          - -c
          - sleep 300

      restartPolicy: Never